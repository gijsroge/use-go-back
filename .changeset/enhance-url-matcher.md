---
"use-go-back": minor
---

Enhanced `targetPathname` matcher function to receive the full URL object instead of just the pathname string. This allows matching based on pathname, origin, search params, hash, and other URL properties.
